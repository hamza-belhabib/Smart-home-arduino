# Arduino source code

Place the corresponding `.ino` source file(s) in each module folder.

Example:

```text
security_pir/
└── security_pir.ino
```

Use the original project code here; do not replace it with invented or
simplified code.
